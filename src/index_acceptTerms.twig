{% extends "assets/template.twig" %}

{% block content %}
<div class="row">
  <div class="col-lg-6 offset-lg-3 col-md-12" style="padding: 5px;">
      <div class="card card-default">
          <div class="card-body" style="text-align: center;">
<p>Пожалуйста, подтвердите, что вы прочитали и принимаете наши
	<a href="{{ CONFIG.LINKS_TERMSOFSERVICEURL }}" target="_blank">условия</a>
</p>

              <button id="submitButton" class="btn btn-default">Принимаю</button>

          </div>
          <div class="card-footer">
<a href="{{ CONFIG.LINKS_SUPPORTURL }}">Связаться с поддержкой</a>


          </div>
          <script>
              $(document).ready(function () {
                  $('#submitButton').click(function () {
                    ajaxcall("account/acceptTOS.php", {}, function (data) {
                        location.reload();
                    });
                  });
              });
          </script>
      </div>
  </div>
</div>
{% endblock %}
